The Book Clerk inventory management program enabled users to add, update, delete, search, and view book records. 
It implements core SQLite functionality using SQL commands such as CREATE, INSERT, UPDATE, SELECT, INNER JOIN, and DROP. 
This task gave me practical experience in database design, data manipulation, and query operations within a real-world application context.
